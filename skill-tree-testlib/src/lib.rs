//! Test infrastructure for skill-tree integration tests.
//! Exposes fixture builders and assertion helpers.
//! Always imported as a dev-dependency — never in production code.

pub mod assertion;
pub mod fixtures;